function f(){ // "function declaration" (because: the starting of function is the keyword `function`)

    // its a funcn.
}

let a = function gun(){ // "function expression" (because: the starting of function is the keyword `let`)
    // okay it also a func 
}

let b = function(){ // "function expression" (because: the starting of function is the keyword `let`)
    // some more impl
}

(function x(){ // "function expression" (because: the starting of function is the keyword `(`)
    //  can you stop it?

    // its IIFE
})

(function(){ // "function expression" 
    // mannn stop it !!

    // its IIFE
})

let y = () => {
    // i am done

    // arrow function
}
